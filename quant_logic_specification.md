# Upbit ATR AutoPilot — 최신 퀀트 로직 명세

> 기준 구현: `server/strategy/atrStrategyCore.ts`, `server/strategy/atrEngine.ts`, `server/risk/globalRiskGovernor.ts`, `server/position/positionManager.ts`
>
> 대상: Upbit KRW 현물 자동매매
>
> 갱신일: 2026-08-22

이 문서는 현재 서버가 실제로 실행하는 규칙을 기준으로 한다. 자동 전략 주문은 신호 생성 후 글로벌 리스크 거버너의 봇 상태, 시세 상태, 미체결 주문, 가용 잔고, 일일 손실 및 최대 노출 검사를 다시 통과해야 한다. 수동 주문은 전략 신호를 거치지 않고 별도 안전 검사 경로를 사용한다.

## 1. 핵심 원칙

- 한 틱에서 여러 조건이 겹쳐도 우선순위가 가장 높은 신호 하나만 반환한다.
- 보호성 매도는 대기 중인 매수 주문이 있어도 허용하되 동일 보호 매도의 중복 발주는 차단한다.
- 자동 신규 매수는 봇이 `RUNNING`, 시세가 `LIVE`이고 쿨다운과 일일 손실 차단이 해제된 상태에서만 가능하다.
- AutoPilot 1 Unit은 총자산 기준 BULL 20%, SIDEWAYS 18%, BEAR 10%다. AutoPilot OFF에서는 `orderRatio`를 사용한다.
- 최종 매수 예산은 `min(목표 예산, 가용 KRW × 98%, 잔여 노출 한도)`이며, 기본 최대 코인 노출은 총자산의 85%다.
- 부분 체결은 거래소 누적량이 아니라 새로 늘어난 증분 체결량만 포지션에 반영한다.

## 2. 지표와 시장 국면

### 2.1 입력과 적응형 지표

- Baseline과 ATR: Upbit 1분봉 기반
- 상위 추세: 15분봉 `BULL | SIDEWAYS | BEAR`
- 단기 기울기: 최근 5개 가격 평균과 최근 15개 평균의 차이를 중기 평균 대비 백분율로 계산
- RSI: 1분봉 종가와 최신 틱을 이용한 RSI(14)
- 거래량 배수: 최신 1분봉 거래량 ÷ 최근 거래량 평균
- 변동성 비율: `ATR / Baseline × 100`
- 유효 ATR: `max(실제 ATR, 5,000 KRW, 현재가 × 0.25%)`

가격 이력이 15개 미만이면 사용자 파라미터와 SIDEWAYS 기본값으로 폴백한다.

### 2.2 국면 판정

BULL은 다음 중 하나를 만족할 때다.

- 15분 추세가 BULL이고 현재가가 Baseline 위
- 현재가가 Baseline 대비 +0.20% 이상이고 15분 추세가 BEAR가 아님
- 단기 기울기 > +0.10%, 현재가가 Baseline 대비 +0.05% 초과이고 15분 추세가 BEAR가 아님

BEAR는 다음 중 하나를 만족할 때다.

- 15분 추세가 BEAR이고 현재가가 Baseline 아래
- 현재가가 Baseline 대비 -0.20% 이하이고 15분 추세가 BULL이 아님
- 단기 기울기 < -0.10%, 현재가가 Baseline 대비 -0.05% 미만이고 15분 추세가 BULL이 아님

나머지는 SIDEWAYS다. 예외적으로 양쪽 조건이 모두 참이면 먼저 평가하는 BULL이 우선한다.

### 2.2-a SIDEWAYS 하위 국면

최종 국면이 SIDEWAYS여도 상위 15분 추세에 따라 아래처럼 내부적으로 분리한다. 화면에는 각 하위 국면 이름을 표시한다.

| 하위 국면 | 판정 | 전략 처리 |
|---|---|---|
| `BULL_PULLBACK` (상승 조정형) | 최종 SIDEWAYS + 15분 BULL | 박스권 전량 익절과 박스 불타기·신규 스캘핑 진입을 하지 않는다. 보유 포지션은 트레일링·수익보호·손절 규칙으로 관리한다. |
| `NEUTRAL_RANGE` (중립 박스형) | 최종 SIDEWAYS + 15분 SIDEWAYS | 기존 박스권 스캘핑 진입·불타기·전량 익절을 적용한다. |
| `BEAR_PAUSE` (하락 정체형) | 최종 SIDEWAYS + 15분 BEAR | 박스권 스캘핑 진입·불타기와 하단 신규 진입을 차단해 현금을 보존한다. 기존 보유 포지션의 보호성 매도와 DCA 규칙은 유지한다. |

### 2.3 AutoPilot 파라미터

| 국면 | ATR 밴드 배수 | 1 Unit | 계산상 DCA 간격 | 스캘핑 밴드 |
|---|---:|---:|---:|---:|
| BULL | 1.8× | 20% | 1.5% | ATR 1.0× |
| SIDEWAYS | 2.4× | 18% | 2.0% | ATR 0.8× |
| BEAR | 3.5× | 10% | 3.0% | ATR 1.4× |

`dynamicDcaStep`은 적응형 상태와 화면에 제공되지만 실제 DCA 체결은 4장의 고정 기준을 사용한다.

- 기본 트레일링 콜백 0.8%, 변동성 비율 > 2.0%이면 1.2%
- 기본 박스권 익절 +0.50%, 변동성 비율 > 2.0%이면 +0.80%

## 3. 신호 우선순위

| 우선순위 | 신호 | 동작 |
|---:|---|---|
| 1 | `ABSOLUTE_STOP_EXIT` | 절대 손절 또는 불타기 수익 보호선 이탈 시 전량 매도 |
| 2 | `EMERGENCY_TREND_CUT` | 급락 시 40% 매도 |
| 2 | `PARTIAL_LOSS_CUT` | 1차 30%, 2차 잔여 수량의 50% 매도 |
| 3 | `SCALP_TAKE_PROFIT` | SIDEWAYS 목표 수익 도달 시 전량 매도 |
| 3 | `SCALP_PARTIAL_TAKE_PROFIT` | 실험실 추세 확장 확인 시 50%만 익절 |
| 3 | `TRAILING_STOP_EXIT` | 1차 50%, 2차 잔여 전량 익절 |
| 4 | `REENTRY_BUY` | 방어 상태에서 급락 진정 확인 후 재매수 |
| 5 | `DCA_BUY` | 고정 단계 DCA 또는 2차 접근 반등 매수 |
| 6 | `PYRAMID_BUY`, `BOX_PYRAMID_BUY` | 상승·박스권 불타기 |
| 6 | `ENTRY_BUY`, `BREAKOUT_BUY` | 저점·반등·돌파 신규 진입 |

## 4. 하락 방어와 DCA

### 4.1 절대 손절과 급락 방어

- 최초 정적 손절가는 `max(하단 밴드 - ATR × stopLossMultiplier, 최초 체결가 × 94%)`로 고정한다.
- DCA 체결 후에는 변경된 평단의 -6.0%로 다시 고정한다.
- 현재가가 정적 손절가 이하이거나 현재 평단 손익률이 -6.0% 이하이면 전량 청산한다.
- 급락 방어는 현재가가 하단 밴드 아래이고 최근 `trendDropWindowSeconds` 변동률이 `-trendDropSpeedThreshold` 이하일 때 발동한다.
- 현재 기본값은 3초 창과 -1.8%이며, 보유 수량 40%를 매도하고 `EMERGENCY_EXIT` 및 60초 쿨다운으로 전환한다.

### 4.2 2단계 부분 손절

- 1차: 현재 평단 -1.0%에서 보유 수량의 30%
- 2차: `partialCutCount === 1`이고 현재 평단 -3.2%에서 남은 수량의 50%
- 각 최초 체결 후 기본 60초 쿨다운을 적용한다.
- 가격이 현재 평단 +0.2% 이상으로 회복하면 부분손절 카운터를 초기화하고 방어 상태를 정상 보유 상태로 되돌린다.
- DCA 체결로 평단이 낮아져도 부분손절 카운터를 초기화한다.

### 4.3 DCA 고정 단계와 예산

| 단계 | 발동 기준 | 총예산 |
|---|---|---:|
| DCA 1차 | 현재 평단 -2.0% | 1.5 Unit |
| DCA 2차 접근 반등 | 최근 20틱 저점이 평단 -3.5%~-4.15%, 저점 대비 +0.7% 반등, 단기 기울기 ≥ +0.05%, 현재가가 평단 -2.5% 이하 | 2차 예산의 40% = 0.8 Unit |
| DCA 2차 본 기준 | 현재 평단 -4.2% | 선매수 없으면 2.0 Unit |
| DCA 2차 잔여 | 선매수 때 고정한 기존 평단 -4.2% 가격 | 60% = 1.2 Unit |
| DCA 3차 | 현재 평단 -5.5% | 1.5 Unit |

- 2차 선매수는 슬롯을 `PARTIALLY_FILLED`로 유지한다.
- 선매수 체결 전 평단의 -4.2% 가격을 `plannedTargetPrice`에 저장하므로 평단이 변해도 잔여 목표가는 이동하지 않는다.
- 선매수 없이 -4.2%에 먼저 도달하면 2.0 Unit 전액을 집행한다.
- 추가 부분 체결은 같은 슬롯 수량에 누적하며 다음 슬롯을 소비하지 않는다.

### 4.4 전략 실험실: DCA 2차 필터 (기본 OFF)

다음은 기존 DCA 2차 접근 반등 선매수에만 추가할 수 있는 독립 실험이다. 모두 OFF일 때는 4.3의 기존 로직과 완전히 같다. 어떤 필터도 2차 본 기준(-4.2%)의 전액 또는 잔여 60% 매수를 막지 않는다.

| 토글 | ON일 때 추가 조건 |
|---|---|
| `DCA 2차 RSI 회복 확인` | RSI ≥ 35 |
| `DCA 2차 거래량 확인` | 거래량 배수 ≥ 1.05 |

## 5. 익절과 불타기

### 5.1 BULL 상승 불타기

BULL 국면, 트레일링 미무장, 트레일링 분배 미시작 상태에서만 허용하며 서버와 전략 모두 최대 2회로 제한한다.

| 단계 | 기준 | 주문 | 후속 보호 |
|---|---|---:|---|
| 추세추종 선행 | BULL, 15분 추세 상승, 수익 +0.70%~+1.00% 미만, 단기 기울기 ≥ +0.10%, 거래량 배수 ≥ 1.30, RSI 52~68 | 0.35 Unit | 기존 불타기 1차로 카운트, 새 전체 평단 +0.1% 수익 보호선 |
| 1차 | 평단 +`pyramidingStepPercent`(기본 +1.5%) | 0.50 Unit | 새 전체 평단 +0.1% 수익 보호선 |
| 2차 | 평단 +`2 × pyramidingStepPercent`(기본 +3.0%) | 0.35 Unit | 추가 불타기 금지, 콜백 최대 0.6% |

- 1차 체결 후 현재가가 `profitLockPrice` 이하이면 우선순위 1 전량 청산한다.
- 트레일링 활성 또는 1차 트레일링 매도가 시작된 포지션은 FLAT까지 불타기를 재개하지 않는다.
- 리스크 거버너도 `trailingExitCount >= 1`인 불타기 주문을 이중 차단한다.
- 추세추종 선행은 트레일링 최소 무장(+1.0%) 전 구간에만 적용하며, 기존 불타기와 `pyramidingCount`를 공유하므로 합산 최대 2회 제한을 넘지 않는다.

### 5.4 전략 실험실: BULL 불타기 필터 (기본 OFF)

다음은 BULL 불타기에만 추가할 수 있는 독립 실험이다. 기존의 BULL 국면·수익률 단계·트레일링 미무장·2단계 제한은 항상 먼저 적용된다.

| 토글 | ON일 때 추가 조건 |
|---|---|
| `불타기 RSI 과열 방지` | RSI 55~68 |
| `불타기 거래량 확인` | 거래량 배수 ≥ 1.15 |

### 5.2 SIDEWAYS 박스권 불타기와 전량 익절

- AutoPilot SIDEWAYS에서 평단 +0.25% 이상이면 최대 2회, 각 0.50 Unit을 추가 매수한다.
- 트레일링 활성 또는 분배 시작 후에는 박스권 불타기도 금지한다.
- 전량 익절 목표는 기본 +0.50%, 고변동성 +0.80%다.
- 박스권 불타기 1회 후 목표는 최소 +0.60%, 2회 후 최소 +0.70%다.

### 5.2-a 전략 실험실: 박스권 익절 · 추세 전환 보호 (기본 OFF)

아래 세 토글은 기존 박스권 익절 규칙을 대체하거나 보완하는 독립 실험이다. 토글을 모두 OFF로 두면 5.2의 전량 익절 및 일반 트레일링 규칙이 그대로 적용된다.

| 토글 | ON일 때 동작 |
|---|---|
| `추세 확장 50% 분할 익절` | SIDEWAYS 익절 목표 도달 시 거래량 배수 ≥ 1.50 및 단기 기울기 ≥ +0.05%이면 전량 대신 50%를 매도한다. 잔량은 평단 +0.2% `profitLockPrice`로 보호한다. DCA·불타기 이력은 소비하지 않는다. |
| `익절 후 추격매수 방지` | 일반 박스권 전량 익절이 체결된 뒤 180초 동안 쿨다운을 적용해 즉시 돌파 매수 등의 신규 진입을 막는다. 부분 익절에는 적용하지 않는다. |
| `추세 확장 트레일링 조기 무장` | 동일한 거래량·기울기 확장 조건에서만 최소 트레일링 무장 수익률을 +1.0%에서 +0.8%로 낮춘다. 상단 ATR 밴드는 계속 충족해야 한다. |

### 5.3 트레일링 익절

- 무장 가격: `max(상단 ATR 밴드, 현재 평단 × 1.010)`이며, `추세 확장 트레일링 조기 무장` 실험 ON 및 확장 조건 충족 시 `현재 평단 × 1.008`
- 청산 가격: `max(최고가 × (1 - 콜백%), 현재 평단 × 1.002)`
- 청산 시점에도 손익률이 최소 +0.1%이고 현재가가 평단보다 높아야 한다.
- 1차는 50%, 2차는 잔여 수량 전량을 청산한다.
- 절반 매도 후 잔여 평가액이 50만 원 미만이면 처음부터 전량 청산한다.
- 각 체결 후 트레일링을 해제하므로 다음 청산에는 무장 조건을 다시 충족해야 한다.
- BULL 불타기 2차 완료 후 콜백은 최대 0.6%다.

## 6. 신규 진입과 재진입

신규 진입은 다음 순서로 평가한다.

1. 하단 밴드: 무포지션, 현재가 ≤ 하단 ATR 밴드, 낙하 칼날 필터 해제
2. 하단 스캘핑: AutoPilot, 비-BULL, 현재가 ≤ `Baseline - ATR × 스캘핑 배수`, 하단 ATR 밴드보다 높음
3. 저점 반등 스캘핑: AutoPilot, 비-BULL, 최근 10개 가격 저점이 Baseline 이하, 저점 대비 +0.15% 반등, 현재가 ≤ Baseline
4. 돌파: 현재가 > Baseline, BULL 또는 기울기 ≥ +0.10%, RSI ≤ 68, 거래량 배수 ≥ 1.15
5. 박스권 상단: AutoPilot SIDEWAYS, `Baseline < 현재가 ≤ Baseline + ATR × 스캘핑 배수`, RSI ≤ 65

낙하 칼날 필터는 단기 기울기 < -0.08%이고 상위 추세 또는 현재 국면이 BEAR일 때 하단 밴드 진입을 막는다.

이유에 `스캘핑 진입`이 포함된 신규 진입은 0.80 Unit, 일반 저점·돌파 진입은 1.0 Unit이다.

### 수동 추가매수

- 포지션 보유 중 수동 추가매수는 총자산 기준 10%, 20%, 30% 중 선택한다. 기본 선택은 10%다.
- 최종 주문 금액은 자동 주문과 동일하게 가용 KRW 98%, 최대 노출 한도 및 업비트 최소 주문 금액 검사를 통과해야 한다.
- 수동 주문은 봇이 `HALTED`이거나 일일 손실 서킷 브레이커가 작동했을 때, 시세가 `LIVE`가 아닐 때, 또는 다른 주문의 체결 확인이 진행 중일 때 차단한다.
- 수동 추가매수는 자동 DCA 슬롯을 소비하지 않으며, 불타기 횟수도 증가시키지 않는다.
- 실제 체결가와 수량으로 평단, Baseline·ATR 기반 보호 밴드, 정적 손절가와 부분손절 카운터를 새로 계산한다.
- 기존 DCA 슬롯 상태(2차 선매수의 고정 잔여 목표가 포함)와 불타기 횟수는 보존한다.
- 트레일링이 무장되었거나 트레일링 분배가 시작된 뒤에는 수동 추가매수를 막는다.

### 방어 후 재진입

- `DEFENSIVE*` 또는 `EMERGENCY_EXIT` 상태에서 쿨다운이 종료되어야 한다.
- 급락 속도가 -0.3% 이상으로 진정되고 현재가가 하단 밴드 위이면 `REENTRY_ALLOWED`로 전환한다.
- 재진입 권한은 한 번만 사용할 수 있다. 리스크 승인 직전에 상태를 `REENTRY_PENDING`으로 원자 전환하므로, 체결 대기·부분 체결·취소 응답 사이에 재진입 주문이 중복 생성될 수 없다.
- 체결이 1원 단위라도 발생하면 실제 체결가·수량으로 평단과 보호 가격을 재계산하고 즉시 `ENTRY_FILLED`로 전환한다. 체결 없이 제출 실패한 경우에만 `REENTRY_ALLOWED`로 복원한다.
- 거래소 잔고 동기화에서 보유 수량이 확인되면 오래된 `REENTRY_ALLOWED`/`REENTRY_PENDING` 상태도 `ENTRY_FILLED`로 정정한다.
- 전량 익절 후 쿨다운은 30초, 손실 청산 후는 180초다.

### 포지션 안전 보정

- 봇이 정지되어 있고 미체결 주문이 0건일 때만 실행할 수 있다.
- 거래소의 실제 보유 수량·평단을 먼저 동기화한 뒤, 최신 Baseline·ATR로 정적 손절·보호 밴드·총원가를 다시 계산한다.
- 부분손절·트레일링·수익 보호·쿨다운은 새 포지션 주기로 초기화한다. 기존 자동 DCA 슬롯 사용 기록은 보존한다.

## 7. 글로벌 리스크 거버너

- 일일 실현 손실이 `dailyMaxLossPercent`를 넘으면 서킷 브레이커를 작동시킨다.
- 봇 상태, 시세 신선도, 쿨다운, 미체결 주문 및 예약 노출을 검사한다.
- 보호성 매도는 미체결 매수가 있어도 허용하되 진행 중인 동일 매도와 중복될 수 없다.
- 목표 예산이 5,000원 미만이면 주문하지 않는다.
- 노출은 주문 제출 시 예약, 체결 시 확정, 거절·오류 시 해제한다.

| 주문 | 1 Unit 대비 배수 |
|---|---:|
| 일반 진입·돌파·재진입 | 1.00 |
| 스캘핑 진입 | 0.80 |
| 박스권 불타기 | 0.50 |
| BULL 불타기 1차 / 2차 | 0.50 / 0.35 |
| DCA 1차 / 2차 / 3차 | 1.50 / 2.00 / 1.50 |

DCA 2차 부분 집행은 2.00 Unit에 40% 또는 60%를 곱한다. 보호성 매도는 현재가의 98.5%를 업비트 호가 단위로 내림한 지정가를 사용한다.

## 8. 포지션과 주문 생명주기

- 포지션, 주문 기록, 처리 신호 ID, 노출 예약과 거래 로그를 파일에 영속화한다.
- 저장은 임시 파일 작성 후 rename하는 원자적 교체 방식을 사용한다.
- 주문 ID와 처리 신호 ID로 재시작 후 중복 발주를 막는다.
- 부분 체결 감시자는 누적 체결량에서 직전 체결량을 뺀 증분 수량과 증분 수수료만 반영한다.
- DCA·불타기·부분손절의 추가 체결은 새 슬롯이나 단계로 계산하지 않는다.
- 전량 청산 시 DCA 슬롯, 불타기, 부분손절, 트레일링, 수익 보호와 가격 기반 상태를 초기화하고 `FLAT`으로 복귀한다. 누적 실현손익·거래 이력은 보존하며, 익절 뒤 30초·손실 청산 뒤 180초 쿨다운을 적용한다.
- 실계좌 수량과 평균 매수가를 거래소 값과 주기적으로 조정한다.

## 9. 성과 집계와 화면

- 일별 손익은 체결 주문을 시간순으로 정렬하고 BUY를 SELL에 FIFO로 대응시킨다.
- SELL 체결일을 손익 귀속일로 사용한다.
- 대응 수량의 매수 수수료와 매도 수수료를 모두 차감한 `netPnl`을 표시한다.
- 매수 기록과 대응되지 않는 매도 수량은 임의 이익을 만들지 않도록 계산에서 제외한다.
- Next Action은 서버의 실제 DCA 슬롯을 우선하며 2차 접근 반등 40%와 잔여 60%를 구분한다.
- BULL 불타기는 0.50/0.35 Unit, 수익 보호 가격과 2차 완료 후 0.6% 콜백을 표시한다.
- 일별 순손익은 최근 7일을 우선 노출하고 스크롤로 이전 기록을 제공한다.
- **전략 실험실** 탭은 DCA 2차와 BULL 불타기 후보 필터를 기본 OFF로 표시한다. 토글은 봇이 정지된 상태에서만 변경할 수 있으며, 서버도 봇 가동 중의 토글 변경 요청을 거부한다.
- 전략 실험 토글은 재시작 후에도 자동 활성화되지 않도록 서버 기본값 OFF다. 실거래 적용 전 매번 사용자가 선택해 켜야 한다.
- 연구 수집기는 봇 가동 여부와 무관하게 가격 변동 중심의 틱과 완결된 1분봉을 `data/research/`에 JSONL로 추가 저장한다. 주문·포지션·API 키 파일에는 접근하거나 영향을 주지 않는다.
- 섀도우 비교는 같은 실제 포지션 상태에서 `기본 규칙`, 각 실험 하나만 ON인 규칙, 현재 활성 규칙의 최상위 신호를 계산한다. 결과가 다를 때만 기록하며 가상 주문·가상 손익을 만들지 않는다. 따라서 이는 **신호 차이 분석**이며 완전한 반사실적 손익 백테스트는 수집 데이터로 별도 실행한다.

## 10. 운영 주의사항

- 이 규칙은 수익을 보장하지 않는다.
- 지정가 하한을 사용해도 급변 시 미체결, 부분 체결, 슬리피지와 API 지연이 발생할 수 있다.
- 실거래 전에 수수료와 예상 슬리피지를 포함한 백테스트와 모의 운용으로 검증해야 한다.
- 문서와 구현이 충돌하면 상단에 명시한 서버 구현 파일을 최종 기준으로 삼는다.
